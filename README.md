## How to run
- Install dependencies with `npm install`.
- Run the server with `node app.js`.
- Open the `index.html` on your web browser.
- Enter a keyword which you would like to search to be scraped.
- The first page of search results will be shown.
